<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Scanning. The document scanner is delivered by Play Services, so no
         CAMERA permission is required: capture happens in its own process. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Indexing runs as a foreground data-sync job. Without these the system
         kills a multi-minute run partway through on API 34+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- allowBackup is off deliberately. Android's default Auto Backup would
         upload databases/dewey.db — the full extracted text and embeddings of
         every document — to the user's Google Drive, which flatly contradicts
         what the README promises about text staying on the device. The 118MB
         encoder under files/ would also exceed the backup quota and make the
         whole backup fail silently. -->
    <application
        android:name=".DeweyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dewey">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Dewey">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager's foreground service must declare the same type the
             worker passes to ForegroundInfo. Declaring the permission alone is
             not enough: the framework compares the requested type against this
             attribute and throws if it is not a subset, which crashes the app
             the moment a long job goes to the foreground. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge"
            xmlns:tools="http://schemas.android.com/tools" />

        <!-- The app supplies its own WorkerFactory, so the default initialiser
             is removed and WorkManager is configured in DeweyApplication. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            xmlns:tools="http://schemas.android.com/tools">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
